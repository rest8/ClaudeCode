# Assets

Place an `icon.ico` (Windows multi-resolution ICO) in this directory to
brand the desktop shortcut. If absent, the installer falls back to the
default Python interpreter icon.

Recommended sizes inside the `.ico`: 16×16, 32×32, 48×48, 256×256.
